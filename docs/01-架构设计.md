# 01-架构设计

本页只描述仓库当前真实存在的组件，不引入额外抽象层。

## 组件分层

| 层级 | 文件 | 职责 |
|------|------|------|
| 配置层 | `galay-ssl/ssl/ssl_context.h` | 持有 `SSL_CTX`，加载证书/CA，设置验证、TLS 版本、Cipher、ALPN、Session 策略 |
| 连接层 | `galay-ssl/ssl/ssl_engine.h` | 持有单连接 `SSL*`，通过 Memory BIO 驱动握手、读写、shutdown |
| 协程层 | `galay-ssl/async/ssl_socket.h`（稳定入口）、`galay-ssl/async/detail/awaitable.h`（内部支撑） | 将 `SslEngine` 与 `galay-kernel` 的非阻塞 IO/协程 awaitable 绑定 |
| 错误层 | `galay-ssl/common/error.h` | 统一 `SslErrorCode` 与 OpenSSL 错误转换 |
| 公共类型层 | `galay-ssl/common/defn.hpp` | `SslMethod`、`SslVerifyMode`、`SslIOResult` 等枚举 |

## 数据流

`galay-ssl` 的核心设计是把 SSL 状态机与网络 IO 解耦：

1. `SslSocket` 负责网络连接、事件注册与 awaitable 生命周期
2. `SslEngine` 内部通过 `initMemoryBIO()` 创建读写 BIO
3. 网络收到的密文先进入 `feedEncryptedInput()`
4. OpenSSL 解密后，业务数据通过 `read()` 暴露给调用方
5. 业务发送的明文通过 `write()` 进入 OpenSSL
6. OpenSSL 产出的密文通过 `extractEncryptedOutput()` 交给底层 socket 发出

这种设计让握手、收发、shutdown 都可以在非阻塞模式下推进。

## `SslContext` 与 `SslEngine` 的关系

- 一个 `SslContext` 可以复用给多个连接
- 每个连接都需要独立的 `SslEngine`
- `SslContext` 管理的是“共享配置”
- `SslEngine` 管理的是“单连接状态”

这也是为什么：

- `SslContext` 提供 `setALPNProtocols()`、`setSessionCacheMode()`、`setSessionTimeout()`
- `SslEngine` 提供 `setSession()`、`getSession()`、`isSessionReused()`

## `SslSocket` 的职责边界

`SslSocket` 不是简单的 `SSL*` 包装。它额外负责：

- 创建底层 socket
- 与 `galay-kernel` 的 `IOController` 和 awaitable 类型对接
- 提供协程式 `connect()`、`accept()`、`handshake()`、`recv()`、`send()`、`shutdown()`、`close()`

需要特别注意：

- 析构函数不会自动关闭底层 fd
- 调用方应显式执行 `shutdown()` 与 `close()`

## 平台后端选择

根 `CMakeLists.txt` 会按平台选择调度后端宏：

- macOS：`USE_KQUEUE`
- Linux + `liburing` 可用：`USE_IOURING`
- Linux + `-DDISABLE_IOURING=ON` 或未找到 `liburing`：`USE_EPOLL`

因此示例代码会按这些宏选择对应 scheduler 头文件。

## 示例与测试在架构中的位置

| 目录 | 作用 |
|------|------|
| `examples/` | 面向用户的最小运行示例 |
| `test/t1_socket.cc` | API/配置层回归 |
| `test/t2_loopback.cc` | 端到端 loopback smoke |
| `test/t3_policy.cc` | 高级 TLS 能力 smoke（ALPN / Session / TLS policy） |
| `benchmark/` | Echo 场景吞吐/QPS benchmark |

## 当前架构边界

仓库当前具备高级 API，但并非每项都已有独立 E2E target：

- 已有基础 Echo 与 CA 校验回环验证
- 已有 `t3_policy` 覆盖 ALPN、Session 复用、TLS 版本约束与 Cipher 策略
- `loadCAPath()` 已有 `t1_socket` 回归覆盖
- mTLS、`useDefaultCA()` 跨平台等价性、显式超时策略仍缺少独立 E2E target
- Modules 仅在工具链满足条件时提供源码树 target，安装包未导出模块 target
